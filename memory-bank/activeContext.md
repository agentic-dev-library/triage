# Active Context

## Current Status

Part of the jbcom multi-organization ecosystem.

See control-center for ecosystem-wide documentation.
## Session: 2025-12-31
- Restructured `triage` into a monorepo under `packages/`.
- Fixed CI failures in PR #94: build, typecheck, and tests now passing.
- Standardized AI integration using the latest `@ai-sdk` patterns.
- Moved unit tests into respective packages and fixed `ai.test.ts`.
- Fixed CLI shebang issue in `tsup` build.
- Verified organization-wide PR status using provided PAT.
- Pushed all fixes to `feat/marketplace-bots-auto-detect` branch.

